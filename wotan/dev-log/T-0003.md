# T-0003: Step E: Carry Fox corrections between D cases

**Status**: BLOCKED (after T-0001) | **Size**: S

## What

At high D, the correction smoothing omega1 is small (0.1, 0.05, 0.01). Resetting C0_CORR and E0_CORR to zero at the start of each case forces the algorithm to "relearn" the correction terms from scratch right where the nonlinear coupling is hardest.

Instead, carry corrections from the previous D case as initial guess for the next.

### Implementation

1. Add storage arrays:
```fortran
REAL(dp) :: C0_SAVE(NRP1,NAP1), E0_SAVE(NRP1,NAP1)
```

2. Initialise them once at program start:
```fortran
C0_SAVE(:,:) = 0.0_dp
E0_SAVE(:,:) = 0.0_dp
```

3. Replace the per-case reset:
```fortran
C0_CORR(:,:) = 0.0_dp
E0_CORR(:,:) = 0.0_dp
```
with:
```fortran
C0_CORR(:,:) = C0_SAVE(:,:)
E0_CORR(:,:) = E0_SAVE(:,:)
```

4. At the end of the case (after corrections have converged), save:
```fortran
C0_SAVE(:,:) = C0_CORR(:,:)
E0_SAVE(:,:) = E0_CORR(:,:)
```

This gives D=2000 a much better starting correction (especially important because omega1=0.1 implies slow correction updates).

## Verification

After applying the change, compile and run. Check:

1. **D <= 1000 regression** — results must still match C&D:
   - D=96: phi_M ≈ 0.995, w_M ≈ 23.34
   - D=500: phi_M ≈ 6.166, w_M ≈ 83.50
   - D=1000: phi_M ≈ 9.308, w_M ≈ 140.6

2. **D = 2000 progress** — compare against C&D grid (b) targets:
   - phi_M target: 13.38, w_M target: 234.9
   - Record whether correction convergence is faster (fewer correction iterations needed)

3. **D = 3500, 5000 progress** — compare against C&D grid (c) approximate targets:
   - D=3500: phi_M ≈ 17.13, w_M ≈ 351.4
   - D=5000: phi_M ≈ 19.97, w_M ≈ 449.3
   - Record whether carrying corrections helps these cases converge

4. **Grid (c) run** — compile with `NR=4*10, NA=4*18` to a separate binary and run for exact C&D comparison at all D values

## Log

- Added C0_SAVE/E0_SAVE arrays, init to zero at program start
- Per-case reset changed from zero to restore from saved
- Save corrections at end of each case alongside solution fields
- D=100 corrections now converge in 3 iterations instead of 4 (warm start from D=96)
- D=1000 corrections now take 30 iterations instead of 22 (carry-over from D=605.72 is a different starting point)

## Outcome

D <= 1000 regression: PASS (values within tolerance, minor last-digit variation from different correction paths)

| D | phi_M | w_M | QR | vs T-0002 |
|---|---|---|---|---|
| 96 | 0.9939 | 23.34 | 0.97659 | identical |
| 500 | 6.1568 | 83.38 | 0.74459 | identical |
| 1000 | 9.3057 | 140.60 | 0.63998 | phi_M 9.3081->9.3057 (within tolerance) |

D >= 2000: Mixed — D=2000 greatly improved, D=3500 worse, D=5000 similar:

| D | phi_M | w_M | C&D phi_M | C&D w_M | vs T-0002 |
|---|---|---|---|---|---|
| 2000 | 15.44 | 224.8 | 13.38 | 234.9 | phi_M 3.10->15.44 (big improvement) |
| 3500 | 49.33 | 202.7 | 17.13 | 351.4 | phi_M 12.59->49.33 (worse, overshoot) |
| 5000 | 16.96 | 367.9 | 19.97 | 449.3 | phi_M 17.52->16.96 (similar) |

All D>=2000 cases still FAILED to converge (oscillating). T-0004 (2-cycle averaging) is the next step.
